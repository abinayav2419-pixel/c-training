#include <stdio.h>

int main() {
    int n, count = 0;

    scanf("%d", &n);
    for (; n > 0; n = n / 10) {
        
        if ( n % 2 == 0) {
            count++;
        }
    }

    printf("%d\n", count);

    return 0;
}
